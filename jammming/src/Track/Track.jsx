import React from "react";
import styles from './Track.module.css'

function Track() {
    return <h1 className={`${styles.background} ${styles.fontStyle}`} >This is the the Track component</h1>
}

export default Track;