import React from "react";
import styles from './Tracklist.module.css'

function Tracklist() {
    return <h1 className={`${styles.background} ${styles.boxStyle}`} >This is the track list component</h1>
}

export default Tracklist;