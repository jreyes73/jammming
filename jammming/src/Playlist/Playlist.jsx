import React from "react";
import styles from './Playlist.module.css'

function Playlist() {
    return <h1 className={`${styles.background} ${styles.fontStyle}`} >This is the playlist component</h1>
}

export default Playlist;